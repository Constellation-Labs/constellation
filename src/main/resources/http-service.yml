apiVersion: v1
kind: Service
metadata:
  name: constellation-node-rpc
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
    name: akka-http
  selector:
    app: constellation
  type: NodePort