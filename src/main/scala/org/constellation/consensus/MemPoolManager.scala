package org.constellation.consensus

/**
  * Created by Wyatt on 5/22/18.
  */
class MemPoolManager {
  //TODO memPoolManager is effectivey responsible for the routing stuff which assigns relative addresses. This will happen through signing with a key sent from the chainStateManager
  //  def signBundle(keyPair: KeyPair, transaction: TXData) = TX(transaction.signed()(keyPair))
  //  def validTx(tx: TXData): Boolean = true
}
